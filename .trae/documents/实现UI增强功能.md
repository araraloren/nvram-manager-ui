## 实现计划

### 1. 后端修改（src-tauri/src/lib.rs）

1.1 **修改AppConfig结构体**
   - 添加`backup_path`字段，用于存储备份路径
   - 添加状态信息相关字段（`status_level`, `status_message`）

1.2 **更新get_app_config函数**
   - 返回包含`backup_path`的配置
   - 设置默认的状态信息

### 2. 前端修改（src/App.vue）

2.1 **更新类型定义**
   - 更新AppConfig类型，添加`backupPath`字段
   - 添加状态信息相关的类型定义

2.2 **窗口标题修改**
   - 在应用初始化时，通过Tauri API设置窗口标题为"NVRAM管理器 v0.1.0"

2.3 **状态栏调整**
   - 在状态栏左下角显示备份路径
   - 在状态栏右下角显示状态信息，根据类型（提示、警告、错误）显示不同颜色

2.4 **状态信息功能**
   - 添加状态信息的响应式变量
   - 根据状态类型（提示、警告、错误）显示不同颜色

2.5 **CSS样式更新**
   - 为不同类型的状态信息添加不同颜色样式
   - 调整状态栏的样式

### 3. 具体实现细节

3.1 **窗口标题设置**
   - 使用Tauri的`Window` API设置窗口标题
   - 显示"NVRAM管理器 v0.1.0"

3.2 **状态栏布局**
   - 左侧：显示备份路径
   - 右侧：显示状态信息，根据类型显示不同颜色

3.3 **状态信息处理**
   - 支持三种状态类型：提示（绿色）、警告（黄色）、错误（红色）
   - 动态更新状态信息

### 4. 测试验证

- 确保窗口标题显示正确的app名字和版本
- 确保备份路径正确显示在状态栏左下角
- 确保状态栏右下角显示状态信息，根据类型显示不同颜色
- 确保所有功能正常运行

## 预期效果

1. 窗口标题栏显示：NVRAM管理器 v0.1.0
2. 状态栏左下角显示备份路径
3. 状态栏右下角显示状态信息，根据类型显示不同颜色（提示：绿色，警告：黄色，错误：红色）
4. 所有功能正常运行，无错误